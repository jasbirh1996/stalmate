package com.user.vaibhavmodules.reels.utils

import java.util.concurrent.TimeUnit

object SharedConstants {
/*    val MAX_DURATION = TimeUnit.SECONDS.toMillis(60)*/
    val MIN_DURATION = TimeUnit.SECONDS.toMillis(10)
    val REQUEST_CODE_PICK_SONG = 123
}
